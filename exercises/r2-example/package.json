{
  "name": "file-api",
  "version": "1.0.0",
  "description": "R2 File Upload API exercise for Cloudflare Workers Workshop",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "bucket:create": "wrangler r2 bucket create file-uploads",
    "bucket:list": "wrangler r2 bucket list",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
